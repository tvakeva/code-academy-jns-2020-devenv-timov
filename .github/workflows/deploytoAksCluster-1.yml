on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: codeacademy2020containerregistry.azurecr.io
        username: ${{ secrets.acr_codeacademy2020containerregistry_username }}
        password: ${{ secrets.acr_codeacademy2020containerregistry_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/userservice" -f  "userservice/Dockerfile" -t codeacademy2020containerregistry.azurecr.io/codeacademy2020cluster:${{ github.sha }}
        docker push codeacademy2020containerregistry.azurecr.io/codeacademy2020cluster:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_codeacademy2020cluster_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep codeacademy2020cluster887f | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace codeacademy2020cluster887f`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: codeacademy2020cluster887f
        container-registry-url: codeacademy2020containerregistry.azurecr.io
        container-registry-username: ${{ secrets.acr_codeacademy2020containerregistry_username }}
        container-registry-password: ${{ secrets.acr_codeacademy2020containerregistry_password }}
        secret-name: codeacademy2dockerauth
       
    - uses: azure/k8s-deploy@v1
      with:
        namespace: codeacademy2020cluster887f
        manifests: |
          manifests/deployment-3.yml
          manifests/service-3.yml
        images: |
          codeacademy2020containerregistry.azurecr.io/codeacademy2020cluster:${{ github.sha }}
        imagepullsecrets: |
          codeacademy2dockerauth